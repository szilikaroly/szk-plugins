{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "science-monitor",
  "version": "0.3.1",
  "displayName": "Science Monitor",
  "description": "Tracks finished manuscripts by submission: which journal, whether the cover letter exists and whether the package was actually sent, what decision came back, and every reviewer point still unanswered. `/sm:context` pulls a manuscript's full working context into the session in one step; `/sm:review` turns an incoming reviewer letter into tracked, answerable points; `/sm:inbox` reads the connected mailbox for editorial decisions. Local SQLite store plus a self-contained HTML dashboard — nothing leaves the machine.",
  "author": {
    "name": "Dr. Szili Károly"
  },
  "license": "MIT",
  "keywords": [
    "manuscript",
    "submission",
    "peer-review",
    "journal",
    "research"
  ]
}
